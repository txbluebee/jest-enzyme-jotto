/**
 * @function secretdWordReducer
 * @param  {string} state 
 * @param  {object} action - action to be reduced
 * @returns {string} 
 */

export default (state = null, action) => {
  return state;
};
